# Third-party notices

This scaffold currently contains original implementation code only.

Runtime/test dependencies are declared in `pyproject.toml` and should be reviewed before redistribution. Public SDKs, sample apps, and protobuf artifacts should be fetched into `vendor/` or a build cache only after their licenses and terms are reviewed for the intended use.
