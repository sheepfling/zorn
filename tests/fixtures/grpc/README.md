# Golden gRPC Wire Fixtures

This directory is reserved for official generated protobuf wire fixtures used by
the Zorn Lattice compatibility suite.

The fixtures are generated from public SDK/proto clients and must not be
hand-authored. The manifest in `manifest.yaml` defines the required files and
the assertions each fixture must satisfy.

